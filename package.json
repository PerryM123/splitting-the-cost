{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt prepare && nuxt dev",
    "dev:ssl": "nuxt prepare && nuxt dev --https --ssl-cert ./localhost.pem --ssl-key ./localhost-key.pem",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@google-cloud/vision": "^4.0.3",
    "nuxt": "^3.10.0",
    "sass": "^1.70.0",
    "vue": "^3.4.15",
    "vue-router": "^4.2.5"
  },
  "dependencies": {
    "prettier": "^3.2.4",
    "stylelint": "^16.2.1"
  }
}
